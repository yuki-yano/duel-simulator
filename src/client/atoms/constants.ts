// History constants
export const HISTORY_LIMIT = 1000

// Animation constants (re-export from existing constants)
export { ANIM, REPLAY_DELAY } from "@/client/constants/animation"

// Delay constants (re-export for backward compatibility)
export { DELAYS } from "@/client/constants/delays"
export const INITIAL_DOM_WAIT = 50 // Re-exported as constant for backward compatibility
